"use client";import {FormEvent,useState} from "react";
export function DataForm({kind}:{kind:"enquiries"|"careers"}){const[state,setState]=useState<"idle"|"sending"|"done"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());const r=await fetch("/api/"+kind,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});setState(r.ok?"done":"error");if(r.ok)e.currentTarget.reset()}return <form className="data-form" onSubmit={submit}><input name="name" placeholder="Full name" required/><input name="email" type="email" placeholder="Email address" required/><input name="phone" placeholder="Phone number"/>{kind==="careers"?<input name="role" placeholder="Position applied for" required/>:<input name="projectType" placeholder="Project type"/>}<textarea name="message" placeholder={kind==="careers"?"Experience and profile link":"Tell us about your project"} required/><button disabled={state==="sending"}>{state==="sending"?"Sending…":"Submit"}</button>{state==="done"&&<p className="success">Thank you. We received your submission.</p>}{state==="error"&&<p className="error">Submission failed. Please try again.</p>}</form>}
